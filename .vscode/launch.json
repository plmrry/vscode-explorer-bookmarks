{
  "configurations": [
    {
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}",
        "${env:HOME}/Desktop/test-bookmarks"
      ],
      "name": "Run Extension",
      "outFiles": ["${workspaceFolder}/out/**/*.js"],
      "preLaunchTask": "build-and-setup",
      "request": "launch",
      "type": "extensionHost"
    }
  ],
  "version": "0.2.0"
}
